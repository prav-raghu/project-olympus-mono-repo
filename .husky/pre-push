# Only check changesets when pushing to main/release branches
branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ] || [ "$branch" = "release" ]; then
pnpm changeset status --since=main
fi
