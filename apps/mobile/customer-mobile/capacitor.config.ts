import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.projectolympus.customermobile',
  appName: 'Project Olympus',
  webDir: 'dist/customer-mobile/browser',
};

export default config;
