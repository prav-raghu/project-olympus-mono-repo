// [placeholder — services/webhook-delivery.service.spec pending NestJS implementation]
describe('webhook-delivery.service', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
