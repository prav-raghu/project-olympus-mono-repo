// [placeholder — services/webhook-subscription.service.spec pending NestJS implementation]
describe('webhook-subscription.service', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
